doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  link(rel='stylesheet' href='style.css')
  link(href="https://fonts.googleapis.com/css?family=VT323" rel="stylesheet")
  
  title 408 Request Timeout
  #app
    #canvas_container
    #info
      h1|408 Request Timeout The Game
      p|Use the handle to rotate the hour glass. 
      p|Watch out! It is broken. Don't let the sand get out.
      p|Remember - You have limited time.
      button(@click='restartGame()' v-if="gameOver")|Restart
    #gui
      | Time limit {{ timeLimit }}s
      div.range-slider
        input(v-model='currAngle' type='range' min="-20" max="20" class='range-slider__range')
    #timeout(:class="gameOver ? 'shown' : ''")| TIME OUT!
    
  script(src='https://cdnjs.cloudflare.com/ajax/libs/vue/2.5.17/vue.min.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/matter-js/0.12.0/matter.min.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.7.2/p5.min.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.7.2/addons/p5.dom.min.js')
  script(src='sketch.js')
